#!/bin/bash
#
# Prints the current date and time in the format used in blog entries.
#
set -o nounset -o errexit -o pipefail

if [[ "$(uname)" == "Darwin" ]]; then
  date -u +%Y-%m-%dT%H:%M:%SZ
else
  date --utc +%Y-%m-%dT%H:%M:%SZ
fi
